import catalog from "@/t2qcal/lib/native-catalog.json";
export default function ResourcesPage(){const groups=[...new Set(catalog.resources.map(r=>r.group))];return <main className="native-page"><section className="native-hero"><div className="native-eyebrow">{"// NO SIGNAL NEEDED"}</div><h1>THE BOOKS.<br/><em>ON THE JOB.</em></h1><p>{catalog.resources.length} manuals, span tables and codes from your T2QCAL reference library.</p></section><p className="native-footnote">Open a publisher’s manual and use your browser’s download option to keep a copy for offline use. Publisher sign-in and access conditions still apply.</p>{groups.map(group=><section key={group}><h2 className="native-section-label">{catalog.resources.find(r=>r.group===group)?.groupName}</h2><div className="native-group">{catalog.resources.filter(r=>r.group===group).map(r=><a className="native-row" key={r.id} href={r.pdf||r.page} target="_blank" rel="noopener noreferrer"><span><strong>{r.title}</strong><small>{r.publisher} · {r.detail}</small>{r.note&&<small>{r.note}</small>}</span><span style={{flex:"none",color:"#ff5f15",fontSize:12}}>Open ↗</span></a>)}</div></section>)}</main>;}
