# Status — 26 September 2026: redesign, client quote video and the agent upgrade live

The server (Contabo vmi3527516) was suspended for an unpaid bill from ~08:15 UTC 24 Sep until the owner paid on 26 Sep; it came back on reboot with data intact (13 quotes, 3 users, last migration `20260925_materials_barcode.sql`, auth still requiring email confirmation).

## Release
| Commit | What | Rollback |
|---|---|---|
| `3ff5cd1` | New look (owner-only switch), outdoor high-contrast mode, `/ui-kit`, Home/Jobs/More/Calendar, new job page, new quote flow, split settings + Your prices, client quote video (card, public quote page, worker), Inter as a variable font, T2QCAL Measure from the other session (`f9edd72`) | `/srv/t2q/rollback/app-20260926-before-redesign` |

- Backup `/srv/t2q/backups/redesign-20260926/postgres-pre-redesign.dump`; rehearsed on `t2q_release_audit_20260926` (restored as `supabase_admin`, owner `postgres`): `test-public-quote-rpcs`, `test-quote-workflow-rpcs`, `test-quote-video-rpcs`, `tests/teams.sql` all passed; copy dropped.
- Migrations applied and tracked: `20260925_profiles_ui_new_look.sql`, `20260925_quote_videos.sql` (tables `quote_videos`, `quote_video_requests`, functions `request_quote_video`, `claim_quote_video_job`, private bucket `quote-videos` 25 MB).
- Smoke 22/22; log clean.
- **Video worker:** `t2q-video.service` (from `deploy/t2q-video.service`) enabled and running; Chrome Headless Shell (Chromium 149) at `/srv/t2q/remotion-browser/chrome-headless-shell-linux64/chrome-headless-shell`, set as `T2Q_RENDER_BROWSER` in `/srv/t2q/app.env` (backup `app.env.bak-video-20260926`). No system packages were needed. Server sample render: 15 s, 720×1280, 1.0 MB, 77 s.
- **New look:** `profiles.ui_new_look = true` for the owner only; `T2Q_NEW_LOOK_DEFAULT` unset (off) for everyone else.

## Agent upgrade release (`545ebec`) and round 3 (`9292925`)
| Commit | What | Rollback |
|---|---|---|
| `545ebec` | `feat/agents-upgrade`: shared AI client (retries, typed errors, env-overridable models), structured quote output + one repair retry, quote-generation lease, agent monitor coverage, plain agent error messages, prompt caching, the maths rounds (one metres band per path, exact half-up rounding, cents-exact money and deposits, wall/lining/cladding/roofing/fencing/concrete fixes), 47 golden jobs (851 figures), `scripts/run-release-evals.mjs` | `/srv/t2q/rollback/app-20260926-before-agents-upgrade` |
| `9292925` | Round 3, from the release evals: stated material/other prices kept and beat a different library price (matcher, catalogue pass, prompt); tradie-counted voice jobs ("14 sheets of GIB") no longer blocked for sizes; chat/reply agents can't state a dollar figure that isn't on the quote (`src/lib/agents/money-guard.ts`); feature-flagged eval suites only gate while the feature is on | `/srv/t2q/rollback/app-20260926-before-agents-round3` |

- Backup `/srv/t2q/backups/agents-upgrade-20260926/postgres-pre-agents-upgrade.dump`; rehearsed on `t2q_release_audit_20260926`: migration applied twice (idempotent), `test-public-quote-rpcs`, `test-quote-workflow-rpcs`, `test-quote-video-rpcs`, `tests/teams.sql` passed, and a lease rehearsal as the `authenticated` owner (claim, busy, stale takeover, renew, release by stamp, another owner blocked, version unchanged) passed; copy dropped.
- Migration applied and tracked: `20260926_quote_generation_lease.sql` (nullable `quotes.generation_started_at`); the API sees the column.
- **Release evals on the staged build, production key.** `545ebec`: golden 946/946, clarify 6/6, quote 7/8, quote-agent 4/4, scan 1/1 — gate FAIL on guardrails 12/14 and plans 1/2. Neither was caused by that release (chat/reply agents, their models and `pricing.ts` unchanged; plan reader off), so it shipped and round 3 fixed them. `9292925`: golden 946/946, guardrails 14/14, clarify 6/6, quote 8/8, quote-agent 4/4, scan 1/1, plans INFO (feature off) — **gate PASS**.
- Smoke 22/22 after each; no app errors after round 3. One expected error after `545ebec`: a `/login` page opened before the deploy posted a stale Server Action (reload fixes it).
- `t2q-video.service` restarted after each swap so it runs from `/srv/t2q/app`, not the moved directory.
- Release script now stages and builds first (`PHASE=stage`), evals run on the stage, then `PHASE=activate` swaps it in.

## 3D website, stage 1 (`a905e4b`, hidden preview)
The owner's immersive "Talk · Quote · Paid" brief (fact-checked plan: https://claude.ai/artifact/FeLQemxHbjemzYrM5NqWwD). Built at **/site-preview** — `robots: noindex, nofollow`, not linked, iOS App Store shell redirected to `/`. The homepage is unchanged until the owner says swap.

- The whole story is real HTML in order (`src/app/_components/jobsite/JobSiteStory.tsx`, copy in `story.ts`): fact-checked lines only (no "under 60 seconds" — not measured on real quotes; invoices are "marked paid", the app doesn't collect them); real plans from `src/lib/plans.ts`; the FAQ and its JSON-LD.
- One on-demand React Three Fiber canvas, loaded after the page is usable: dawn site with a real 90 × 45 frame at 600 centres (`canvas/frame.ts`), camera through a stud bay to a phone with a live "talk the job through" screen, a flash cut into the phone, a waveform tunnel. Chapters TALK / QUOTE / PAID / Tools down are HTML for now (3D worlds in stages 2–3).
- Device levels (`level.ts`): full / lite (phones) / still (no WebGL, reduced motion, "Pause background motion", data saver, low-end). The still version uses a drawn SVG of the same shot.
- Lazy 3D download: **243 KB gz** (three.js + R3F + scene), none of it on other routes. Only `@react-three/fiber` was added (9.7.0 → 9.8.1, already present via `@remotion/three`); GSAP and Lenis weren't needed — the camera eases the journey (`timeline.ts`, tested) so jump links fly along the route, never through a wall.
- Tests: device levels, the camera timeline, the frame (the camera path is clear of every member), and the rendered story (headings, anchors, plans, FAQ, no unbacked claims, nothing priced in the iOS shell). Full suite 7,094 passed.
- Release: stage built with `npm ci` (package change), smoke 24/24 (22 + `/site-preview` 200 + iOS 307); rollback `/srv/t2q/rollback/app-20260926-before-site-3d-stage1`; `t2q-video` restarted.

## 3D website, stage 2 (`d3de847`, hidden preview): inside the house
The owner asked for the dive into the phone to open into a real house, one step per room (plan: https://claude.ai/artifact/LKTQ5KR7uUN6eLgkt2HYvz). He chose: the rooms as planned, straight from the phone into the house (the waveform tunnel is gone), "it's my build, say so", and the demo's screens for now.

- `HouseWalk.tsx`: Talk (hallway), Draft (living room to the bifolds over the city), Check (raked sarking ceiling), Send (big sliding door), Invoice (finished front entry photo), then the finished deck: "Tools down. Quote sent." Each room: big bold words, the small print that used to be the Talk/Quote/Paid chapters, and the step's screen on a phone labelled "Example job · example figures". "Filmed on one of our builds in Tauranga."
- Rooms hold 1.3 screens and fade into the next; each clip plays once on arrival and holds; nothing loads until the house is near, then one room ahead; phones get 540p cuts; the still version shows a still per room. Computers show the footage as a tall panel with a blurred backdrop (320 px, q40).
- Media (6.8 MB, `public/jobsite/`, cached like other media): `scripts/jobsite-media.sh` cuts the owner's originals, strips all metadata (the photos carried the house's GPS) and sound, and skips the seconds of clip 3 showing a car's number plate. Screens are full-resolution frames of `public/videos/demo-tall.mp4` (the Check screen's 24 hr × $65 = $1,560 matches the draft's 26 hr × $60).
- Live checks: clips 200 video/mp4 (≈0.2–0.95 MB), screens as 19 KB WebP, deck photo 71 KB WebP at 1080 w; smoke 24/24. Rollback `/srv/t2q/rollback/app-20260926-before-site-3d-stage2`.
- Follow-up `ae2667f` (not yet deployed): deep links (`#draft`, `#pricing`) land correctly after the motion layout grows. Offered to the other session to ship with feat/location.

## 3D website, stage 2b (`33b7977`, live as `da27603`): a real phone in every room
The owner found the flat screenshots unprofessional and asked for a real phone floating in each room, playing that step live as a 3D Remotion animation.

- One 3D phone (`canvas/HousePhone.tsx`, model in `canvas/phone-model.ts`): black titanium edge, glass front, camera bump, side buttons, a glass streak and a soft shade, drawn from primitives. It floats over each room's phone slot (walk maths in `house-walk.ts`, now shared with `HouseWalk.tsx`), rides in with the first room, sways while you read, turns round into the next room (the screen changes while its back faces you) and rides away with the last.
- Screens: Remotion `StepScreen` (the demo's chapter as the bare 390 × 844 screen), `node scripts/render-marketing.mjs --only=steps` → `public/jobsite/screens/<step>-600.mp4` (computers) and `-420.mp4` (phones), 185–414 KB each, 3.0 MB in all; `<step>-first.webp` shows until the clip has a frame, `<step>.webp` (the finished screen) is the still version and the fallback when autoplay is refused. Clips load for the room you're in and the next, play once and hold.
- The canvas has a "house" space: the site is hidden and the layer sits over the rooms (under the header, step links and Start button) at up to 2× resolution, set through the Canvas `dpr` prop (R3F re-applies the prop on every render, so `setDpr` alone was undone).
- Still version: a drawn phone with the finished screen; its alt text serves screen readers in every version.
- Found on the way: deep links (`#send`, `#pricing`) landed up to 200 px off on computers because the browser's smooth jump to the #id was still running when the motion layout re-anchored; it is now stopped first and re-checked a frame later (phone and computer, `#talk`/`#send`/`#pricing` exact). The step encodes had two `-vf` (the colour one replaced the scale). The house backdrops' `quality={40}` was silently served as 75 (not in `images.qualities`), so it's gone.
- Lazy 3D download ≈ 254 KB gz (+11 KB). Full suite 7,503 passed after merging the other session's `6425558`; build clean.
- Release over `6425558` (feat/location, the other session's App Store work): smoke 24/24; native UA `/` and `/install` → 307 `/app`; clips 200 `video/mp4` with byte ranges and the media cache; `t2q-video` restarted; journal clean. Rollback `/srv/t2q/rollback/app-20260927-before-site-3d-phone`. Live check in a headless phone browser: `#draft` lands exactly, canvas 780 × 1688, only the Draft and Check clips fetched.

## Next
- 3D website: owner check-in on the floating phone (on his phone). Recordings from a real demo account could replace the demo animations later; the founder photo is still wanted; then the speed/accessibility pass and the swap onto the homepage (stage 4).
- Phase 4 (owner decision): measured model A/B (drawing scan `claude-opus-4-8` → `claude-opus-5`; photo plan off `gpt-4o-mini`) and prompt cleanup (stop asking the model for totals the server recomputes).
- Plan reader: add the seven hand-labelled plan sheets before switching `PLAN_READER_ENABLED` on.
- Owner: sign up `challis836+demo@gmail.com` for the automated checks.
- Remotion licence: free for companies of ≤ 3 people.
