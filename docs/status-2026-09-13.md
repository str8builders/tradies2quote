# Tradies2Quote status — 13 September 2026

## Verified live

- **Production now runs `558e93229bf506c81ac66fca54e11d68a179263f`.** Activated at 2026-09-12 21:51:44 UTC (13 September 09:51:44 NZST) after the owner approved deployment. Public health returned HTTP 200 with `558e93229b` at 21:52:20 UTC. All 19 release files match their reviewed hashes. `TRANSCRIPT_SUMMARY` is absent from the private environment, whose permissions remain 0600.
- Billing database inspected at 21:30:58 UTC: one incomplete checkout/customer row, **zero active subscriptions**, no Stripe subscription ID or paid period on that row. The two processed webhook events are earlier nonfinancial signature diagnostics. **Real paid billing remains unverified.** The owner declined the live card test because funds are unavailable; no payment was submitted.
- Source review confirms checkout and subscription lifecycle events retrieve current Stripe state and call `sync_stripe_subscription`. It records user/customer/subscription identity, plan, status, current period end, creation and observation timestamps in `public.subscriptions`. `invoice.paid` and `invoice.payment_failed` are acknowledged and recorded in the completed-event ledger; they do not directly change entitlement. The ledger is written after successful processing to allow retries. Successful HTTP requests are not explicitly logged by this route, so an absent journal error would not prove HTTP 200. A real test needs Stripe delivery status plus the processed ledger and subscription row.
- Earliest non-owner trial end in the current database: 2026-09-18 21:31:50 UTC, which is **19 September 09:31:50 NZST**.
- Vercel CLI listed **six active legacy schedules** for `challis-projects/tradies-nz`: engagement 19:00 UTC; trial emails 20:00 UTC; weather evening 07:00 UTC; weather morning 18:00 UTC; weather pre-job 05:00 UTC; weekly digest Sunday 20:00 UTC. No Vercel or Stripe dashboard was entered this session.
- All four production timers are enabled and explicitly use `Pacific/Auckland`. Next runs observed for 14 September NZST: error digest 06:45:12, weekly digest 07:00:47, trial emails 09:00:25, engagement 09:15:48. Each has up to 60 seconds of configured random delay.
- Auth host adjustment completed at 21:27 UTC. Effective `GOTRUE_MAILER_EXTERNAL_HOSTS` matches `api.tradies2quote.com`. The authenticated gateway settings probe returned HTTP 200 at 21:31:59 UTC. Zero external-host log entries were observed after the auth restart, compared with 365 in the preceding hour. This was a settings probe, not an email-send test.
- Existing synthetic audit quote opened in headless Chromium in `Pacific/Auckland` at 21:33:39 UTC and `Europe/Berlin` at 21:33:44 UTC. Both returned HTTP 200, displayed the quote, and produced zero console/page/hydration errors. Browser-reported time zones matched. This was the prior audit quote, **not** the affected `HE0ZITPJE9…` quote. The database still contains the single earlier #418 event at 18:37:14 UTC. No public quote application code was changed on this evidence. After deployment, the same synthetic quote again passed both zones at 21:56:12 / 21:56:16 UTC: HTTP 200, quote visible, zero console/page/hydration errors.
- Signed-in Chrome dashboard and the existing audit quote opened successfully after activation. Both Save / back up and View PDF correctly block the missing business name, display the actionable message and link to Settings; the Settings link worked. No email was sent. The email/text guards passed route tests on the server, but were not exercised as live sends. No new application-error event was recorded in the initial post-deployment check.
- Live release inventory: **18 configured, four blocked** — client plan reader, quote deposits, client deposits, and Twilio SMS. These are the expected existing gates. This configuration check exits nonzero because those gates are blocked; it is not an all-features pass.

## Changed in this session

- `9115ebb` — restores hosted Anthropic transcript summaries with the configured model/default, a 50-second timeout, existing JSON parser and `callAnthropic` test seam. Local-provider behavior remains covered. The off override is respected. Hosted request shape follows the [Anthropic Messages API](https://platform.claude.com/docs/en/api/http/messages/create). No paid provider request was made for these tests.
- `8ef26ff` — blocks quote email, text and owner PDF creation for a missing or whitespace-only business name before document generation/upload/delivery. Provides an actionable Settings link in send errors, PDF download errors and the PDF viewer. The shared PDF generator rejects blank identity too. Database lookup failures get a retry message. The dashboard nudge remains.
- `f118b77` — adds the current snapshot branch to the existing GitHub lint/test trigger. The workflow adds no deployment action.
- `558e932` — adds the owner-run headless time-zone audit script and Playwright as a development dependency. Patches Vitest to 4.1.11 following its [maintainer security advisory](https://github.com/vitest-dev/vitest/security/advisories/GHSA-82fw-gwwq-j7x9). Installation reported zero npm vulnerabilities. The installed npm 10 updater failed internally; a one-off npm 11 invocation completed without changing global npm or Git configuration.
- Local checks: **2,384 tests passed, 25 skipped** across 175 passing and six skipped test files. The skips are the existing provider/evaluation gates. Full lint passed. The production build and final standalone `npx tsc --noEmit` both passed on the completed source. Candidate `558e93229bf506c81ac66fca54e11d68a179263f` also passed the complete server suite (2,384 tests; 25 skipped), lint, production build and standalone TypeScript with a fresh Linux dependency install before activation.
- The approved GitHub push failed with `Permission denied (publickey)`. No key was created and no persistent Git configuration was changed. No GitHub Actions result exists for these unpushed commits. Commits use the existing Codex author identity supplied for each invocation because local author configuration was missing.
- Auth configuration backup: `/srv/t2q/backups/auth-host-20260912T212717Z` (restricted permissions). Added the Compose host mapping and its private environment setting, validated Compose and recreated **only** auth with `--no-deps`.
- Approved cleanup removed exactly the 13 paths in the owner approval list. Disk free space increased by **21,114,056,704 bytes** at 21:29:11 UTC. Kept `/srv/t2q/rollback/app-20260913-before-welcome` and `/srv/t2q/rollback/app-20260913-before-teams`, the active application, database backups and release log files. No Qwen change was made: the owner approved push/cleanup but did not select a Qwen action. At initial inspection Qwen held 4.4 GiB; server available memory was 6,800 MiB and swap usage 2,045/2,047 MiB.
- No marketing landing files were modified. Built the approved release in `/srv/t2q/releases/followup-20260913-558e932`, then moved it to `/srv/t2q/app`. The previous live app is retained at `/srv/t2q/rollback/app-20260913-before-summary-business`; the private environment backup is `/srv/t2q/backups/followup-20260913-558e932/app.env.before`. The previous team database backup remained readable (1,048 archive-list lines); this release applied no database migration. Corrected one absent-file entry in the local release manifest before staging; application source was unchanged.

## Still open with owner actions

1. **Paid billing remains unverified; no purchase is required now.** When affordable, use a normal customer account on `/app/upgrade`, then inspect Settings. Confirm Stripe delivery HTTP 200 and the active database row, cancel through the portal and verify the new subscription event/row. Cancellation at period end should preserve paid access until that date. The Gmail owner account bypasses billing and is unsuitable for this test. No refund was promised or performed.
2. **Confirm the live Stripe webhook in the dashboard:** exact URL `https://tradies2quote.com/api/stripe/webhook`, enabled, with `checkout.session.completed`, `customer.subscription.created`, `customer.subscription.updated`, `customer.subscription.deleted`, `invoice.paid`, and `invoice.payment_failed`. The restricted key cannot list endpoints. Owner confirmation has not yet arrived.
3. **Pause the legacy Vercel project or remove its six schedules**, then tell Codex so the CLI can recheck zero jobs/paused status. Do not delete the project; it holds domain registration/DNS. This remains an unresolved duplicate/stale email risk identified by the supplied audit.
4. **Push from an environment with the existing GitHub SSH access:** `cd /Users/admin/Desktop/tradies2quote && git push origin snapshot/production-2026-07-26`. Then check the CI result. The branch includes the added CI trigger.
5. **Provide the full affected public quote URL** to reproduce #418 in both time zones. The repeatable script is `scripts/audit-public-quote-hydration.mjs`; it uses `T2Q_PUBLIC_QUOTE_URL` and optional `T2Q_HYDRATION_OUTPUT`. It redacts the token in JSON output. The original affected route is still unverified.
6. **Choose a Qwen action only if desired:** leave unchanged; reduce context to 8192 and restart; or stop and disable. It may serve STR8 HUB, so no change was inferred from the general “ok”. Record new before/after memory and load if changed.
7. **Complete the real business name in Settings and generate a fresh quote.** The owner was asked to do this in the signed-in Chrome session after activation. Codex must then inspect that quote's stored `transcript.summary`. The new PDF guard is verified live; a fresh production summary is still awaiting that owner-generated quote.
8. Plan reader stays gated pending representative evaluations; deposits require their separate Connect/webhook release; Twilio remains unconfigured. The previous Resend invoice was paid and was not charged again. Other provider balances and renewal dates were not re-audited in this session.

## Addendum — Claude session, 13 September ~11:00 NZST

- **Legacy Vercel project paused** via API on owner request; `paused: true`, old deployment returns 503 DEPLOYMENT_PAUSED. Its six schedules can no longer run. Unpause: `POST /v1/projects/prj_WOYaQgYjMptslW56IiSiySLppivV/unpause?teamId=team_R50u6p5pgkoqnKltRKhfBJ0D`.
- **React #418 closed.** `scripts/audit-public-quote-hydration.mjs` run against the affected quote in Pacific/Auckland and Europe/Berlin: status 200, quote visible, zero console/page errors, zero hydration errors in both. Treated as a one-off client-side event (extension or stale chunk during the evening deploys). No code change.
- **Stripe webhook handler verified live, zero cost.** A synthetic `invoice.paid` event signed with the server's `STRIPE_WEBHOOK_SECRET` was accepted (200, ledger row written, row then removed); a bad signature was rejected (400). Remaining unknown is only Stripe-side delivery, which the next item proves.
- **Free end-to-end billing test prepared.** A live Checkout Session for the hotmail test account: Solo price, 1-day trial, no card required (`payment_method_collection=if_required`), auto-cancels when the trial ends without a payment method, `amount_total` 0. Metadata `t2q_user_id`/`t2q_plan=solo`/`t2q_audit`. Owner opens the link, clicks Start trial; then verify `public.subscriptions` shows `trialing`/`solo` for that user and `stripe_webhook_events` has the `checkout.session.completed` + `customer.subscription.created` ids. Expires in 24 h if unused.
- **GitHub push still blocked**: `~/.ssh/github_str8builders_ed25519` exists but GitHub rejects it (publickey). Owner must add the public key (fingerprint `SHA256:cw+qKwVM9eH8crzSFgtSYruep2qVrU80nnvzH92e++4`) as a deploy key with write access on `str8builders/tradies2quote` or to their GitHub account, then `git push origin snapshot/production-2026-07-26`.
- **Qwen left running deliberately**: 110 completions in the last 7 days, last one this morning; `/srv/nursemate/app.env` and the coder service point at it. Stopping it would break NurseMate's text features. Memory pressure stays; the real fix is more RAM or moving Qwen off this box.

### Billing verified end to end — 13 September ~11:45 NZST
Owner completed the $0 no-card trial checkout. Stripe: session `complete`, subscription `sub_1UEzec…` status `trialing`, plan metadata `solo`, trial ends 2026-09-13 22:44 UTC. Server: `public.subscriptions` for the hotmail account updated to `trialing` / `solo` with the Stripe subscription id and `current_period_end` 2026-09-13 22:44 UTC, three seconds after checkout — so the live webhook endpoint, its signing secret, `sync_stripe_subscription` and plan mapping all work. Expected follow-up: when the trial ends without a payment method, Stripe cancels and sends `customer.subscription.deleted`; the row should flip to `canceled` and the account falls back to its normal 7-day signup trial. Check after 2026-09-14 10:45 NZST. The single "No signatures found" error at 22:32 UTC was the deliberate forged-signature probe, not a real failure.

### Agents audit and flag restoration — 13 September ~13:40 NZST
- All 30 model-backed / rule-based agents inventoried. Anthropic fleet is current (claude-sonnet-5 default, claude-haiku-4-5 fast, claude-opus-4-8 deep/vision); OpenAI transcription is gpt-4o-transcribe; the only older-generation ID is gpt-4o-mini in the Photo/Plan agent (works, worth an eval later). Live runtime proven by the "Test" agent run and the two real quotes.
- Production had only three feature flags set; the old Vercel deployment carried more. Restored on the server: `NZ_COMPLIANCE_REVIEW_ENABLED=true` (deterministic NZ compliance layer on generated quotes), `KITS_ENABLED=true` (kits tables exist), `QUOTE_VERIFY_ENABLED=true` (second-opinion critic on the owner Quote Generation Agent), `TRADIE_BRAIN_ENABLED=true` (`tradie_memories` table + unique index present). Left off deliberately: `REVIEWS_ENABLED` (emails real clients for reviews — owner decision), `T2Q_WEATHER_PLANNING` (Pat/Willa, never enabled before), `SUGGEST_PRICE_AGENT_ENABLED`, `PLAN_READER_ENABLED`, `PAYMENTS_ENABLED`. Backup: `/srv/t2q/app.env.bak-flags-20260913`.
- Bug fixed and deployed (commit `350a817`): agent monitor showed 9 runs stuck "running" because run.start upsert and run.finish update raced. Run-row writes are now queued per run id; the 9 rows were closed from their existing finish events. Rollback `/srv/t2q/rollback/app-20260913-before-agents`; oldest rollback pruned.
- Owner-only tools that still require the local model: error-digest AI triage and the monitor "Diagnose" button (they return no diagnosis / 503 while `TEXT_AI_PROVIDER=anthropic`). Cosmetic; add an Anthropic path if wanted.

### Public quote requests — built and live, 13 September ~14:10 NZST
Commit `a269339` (plus the matcher follow-up below). Live end-to-end test on the owner's hotmail test account (slug `t2q-owner-test`): a client submitted the public form at `/r/t2q-owner-test` → client record created → draft quote created → request row `generated` 39 s later → quote has 9 lines, total $862.50, client block filled → "New quote request from Sarah Testclient" email delivered to the tradie → no app errors. Push notification was attempted (no subscriptions on the account yet).
- Public page `/r/[slug]`, API `POST /api/requests/[slug]` (validation, honeypot, crawler check, per-IP 10/day, per-tradie 40/day, moderation), Settings card "Your request link" (turn on/off, copy, open), list page `/app/requests`. Generation runs after the response via `after()`; capped at 30 auto-generations per tradie per day; skipped (request still recorded) when the tradie's subscription can't create quotes.
- Migration `20260913_quote_requests.sql` applied to production (backup `/srv/t2q/backups/requests-20260913/pre-requests.dump`), recorded in `_applied_migrations`.
- Generation pipeline extracted to `src/lib/quote-generation/run.ts`; the tradie route now delegates to it. Every query scoped by user id; the admin-client path is used only by the intake.
- Found during the live test: the catalogue matcher used the cookie client and was denied for anonymous requests, so matched prices fell back to unmatched. Fixed by threading an `asAdmin` flag through the matcher.
- Not built: client photo uploads (photo storage is gated to team plans via an RPC), the conversational clarifying-questions box, a dashboard badge (dashboard page is mid-edit by Codex). Rollback `/srv/t2q/rollback/app-20260913-before-requests`.
